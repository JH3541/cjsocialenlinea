var customerSupportModalController = (function() {
  var identifiers = {
    helpModalHeader: 'bb-customer-support-modal-header',
    helpModalBody: 'bb-customer-support-modal-body',
  };

  function openModal() {
    if (modalController) {
      modalController.showModal(identifiers.helpModalHeader, identifiers.helpModalBody);
    }
  }

  return {
    openModal: openModal
  };
}());